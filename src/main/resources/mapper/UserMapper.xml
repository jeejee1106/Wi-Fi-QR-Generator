<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.project.user.mapper.UserMapper">

    <!-- 회원 저장 -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userSeq" parameterType="com.example.project.user.dto.request.UserJoinReq">  <!-- parameterType="com.example.project.user.dto.request.UserJoinReq" -->

        INSERT INTO users (
            email,
            password,
            name
        )
        VALUES (
            #{email},
            #{password},
            #{name}
        )
    </insert>

</mapper>
